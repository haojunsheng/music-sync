[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 固定 basetemp 到项目内：受限环境（沙箱）下对系统临时目录执行
# mkdir(exist_ok=True) 会失败，而固定 basetemp 时 pytest 会先清空再重建，可绕开该问题。
# 注意：请从项目根目录执行 pytest，使相对路径 .pytest_tmp 落在项目内。
addopts = -ra --basetemp=.pytest_tmp
